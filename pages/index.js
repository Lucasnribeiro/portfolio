import { Grid } from '@mantine/core';

export default function HomePage() {
  return (
    <Grid justify="center" align="center">
      <Grid.Col span={4}>Currently being built.</Grid.Col>
    </Grid>
  );
}